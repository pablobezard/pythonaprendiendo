x = (1 , 2, 3 ) #forma clasica
xl = (1,) #asi se escribe un tuple de un elemento 
print(x)

list

y = tuple((1 , 2, 3)) #forma especifica 

#para ver todo lo que podemos hacer con una tupla 
# print(dir(x))

#print(x[4]) ver elemento especifico de una tuple 

#del x # eliminar un valor no es posible en una tuple ya que es inmutable no puede cambiar

locations= {
    (35.12312, 39.000): tokyo, 
    (23.31313, 23.241) : New yor }